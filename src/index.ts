/**
 * Greet a specific person
 * @param name Someone's name
 * @returns a very polite greeting :)
 */
export function greet2(name: string): string {
	return `Hello, ${name}!`;
}

console.log(greet2('World'));
